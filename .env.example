#------------------------------------------------------------------------------
# SMARTER_ROOT_DOMAIN (REQUIRED) -> smarter_settings.root_domain
# The root domain for your Smarter deployment.
# Controls the default domain used for the Smarter web console and for LLMClients.
#
# IMPORTANT: an AWS Route53 Hosted Zone for this domain must already exist prior
# to deploying Smarter LLMClients that use this domain. The AWS keypair used by
# Smarter must have permissions to create and manage Route53 DNS records
# in this domain's hosted zone.
#------------------------------------------------------------------------------
# SMARTER_ROOT_DOMAIN=example.com

###############################################################################
# The Smarter Project environment variables.
#
# Copy this file to a new file named '.env' in the root of the project and set
# the required values. Do NOT commit the .env file to version control! .gitignore
# is preconfigured to ignore the .env file, as well as other sensitive files.
#
# NOTE: for local development environments it might be necessary for you
# to run `source .env` in order for your shell and your integrated development
# environment (IDE) to pick up the environment variables defined in this file.
# If you are using VS Code, you may need to restart VS Code after sourcing the
# .env file so that the environment variables -- namely PYTHONPATH -- are picked
# up correctly.
#
# The .env file is consumed automatically by Docker Compose as well as internally
# in the application, by python-dotenv and django-environ when running the
# Django application. Note that any environment variables set in the actual
# environment (e.g. in your shell or CI/CD environment) will override
# the values set in this .env file.
#
# For more information, see:
#  - https://github.com/joke2k/django-environ/
#  - https://pypi.org/project/python-dotenv/
#  - smarter/settings/base.py
#  - smarter/settings/base_aws.py
#  - smarter/common/conf.py
###############################################################################
# SMARTER_PLATFORM_SUBDOMAIN=platform

###############################################################################
# AWS settings. (REQUIRED) These are required for deploying to AWS cloud
# environments. Note that Smarter interacts with AWS services even when
# running locally in developer mode, so these settings are required even
# for local development. For example, Smarter deploys LLMClients to AWS EKS
# 'alpha' environment as a proxy for local development. AWS services are
# called asynchronously via Celery tasks in the 'worker' container, so if
# AWS credentials are not set, the tasks will fail but the main application will
# continue to run.
#
# The AWS keypair credentials are consumed automatically by boto3 and
# AWS SDKs as environment variables. No further configuration is required.
#
# IMPORTANT: These AWS credentials must have sufficient permissions
# to create and manage AWS resources used by the Smarter platform,
# including but not limited to:
#  - AWS ECR
#  - AWS EC2
#  - AWS EKS
#  - AWS RDS
#  - AWS S3
#  - AWS Route53
#  - AWS IAM
#
# Unit tests that require connecivity to AWS will be skipped if these
# credentials are not set, but note that many integration tests and
# tests may nonetheless attempt to interact with AWS services including
# but not limited to EKS, RDS and Route53.
#
###############################################################################
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=SET-ME-PLEASE
# AWS_SECRET_ACCESS_KEY=SET-ME-PLEASE

###############################################################################
# Development settings. These are required for local development.
# The default values here are suitable in most cases.
###############################################################################

#------------------------------------------------------------------------------
# GH_TOKEN (aka PAT). (OPTIONAL) GitHub Personal Access Token. This is an optional setting
# that is used only if you plan to contribute to the Smarter
# project on GitHub.
#------------------------------------------------------------------------------
# GH_TOKEN=SET-ME-PLEASE
GITHUB_TOKEN=$GH_TOKEN


#------------------------------------------------------------------------------
# SMARTER_OPENAI_API_KEY (REQUIRED) -> smarter_settings.openai_api_key
# OpenAI API key. This is the default LLM API key
# and is required for minimum use of the Smarter platform.
#
# see: https://platform.openai.com/api-keys
#------------------------------------------------------------------------------
SMARTER_OPENAI_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# PYTHONPATH. (REQUIRED) We need to explicitly set the PYTHONPATH
# for Pylint so that it can find the smarter module
# in your local file system when running pylint, flake8, or Cornflakes
# from inside of VS Code. This is unrelated to actually
# running the Django app from Docker, which sets the
# PYTHONPATH in the Dockerfile.
#
# This default value are probably what you want:
#------------------------------------------------------------------------------
PYTHONPATH=./venv:./smarter

#------------------------------------------------------------------------------
# ENV_LOADED. (REQUIRED) Internal environment variable used to confirm that this file has been
# loaded.
#------------------------------------------------------------------------------
ENV_LOADED=true

#------------------------------------------------------------------------------
# ENVIRONMENT. (REQUIRED) Environment type. Set to 'local' for local development.
#------------------------------------------------------------------------------
ENVIRONMENT=local


#------------------------------------------------------------------------------
# NODE_ENV. (REQUIRED) Node environment type for react component development.
# Set to 'development' for local development.
#
# To create production builds either do not use this variable, or, set it to
# NODE_ENV=production
#------------------------------------------------------------------------------
NODE_ENV=development

#------------------------------------------------------------------------------
# DEBUG_MODE. (REQUIRED) Used locally to control logging verbosity and also determines
# how static files are served.
#------------------------------------------------------------------------------
DEBUG_MODE=true

#------------------------------------------------------------------------------
# DOCKER_COLLECT_STATIC_FILES. (REQUIRED) Used inside of Dockerfile to control whether
# we run the collectstatic management command during the build process. This is
# used to speed up the build process during local development when we can rely
# on locally cached static files, and we therefore do not need to collect static
# files during every build.
#------------------------------------------------------------------------------
DOCKER_COLLECT_STATIC_FILES=true

#------------------------------------------------------------------------------
# DOCKER_REACT_REMOTE_CACHE_BUSTER. (OPTIONAL) Used as a build argument in the Dockerfile to
# force downloading remote manifest.json files for React components from the
# CDN, which inturn forces rebuilding the React components.
#
# This should be unset (ie not an empty string) if you are not using a CDN
# to host your React frontend assets. If you are using a CDN, set this to the base URL of the CDN from which to download the manifest.json files for the React components, which inturn forces rebuilding the React components during the Docker build process.
#------------------------------------------------------------------------------
DOCKER_REACT_REMOTE_CACHE_BUSTER=

#------------------------------------------------------------------------------
# DOCKER_REACT_REMOTE_CDN_URL. (OPTIONAL) Used as a build argument in the Dockerfile to set the
# base URL for the CDN from which to download React component manifest.json
# files and assets.
#
# This should be unset (ie not an empty string) if you are not using a CDN
# to host your React frontend assets. If you are using a CDN, set this to the base URL of the CDN from which to download the manifest.json files for the React components, which inturn forces rebuilding the React components during the Docker build process.
#------------------------------------------------------------------------------
DOCKER_REACT_REMOTE_CDN_URL=


#------------------------------------------------------------------------------
# SMARTER_DEVELOPER_MODE. (REQUIRED) Used inside of Django settings to enable
# tools that should only be used locally during development, such as
# the Django Debug Toolbar, Django Extensions, and other debug features.
#
# Also serves Keene static files.
#------------------------------------------------------------------------------
# SMARTER_DEVELOPER_MODE=false

#------------------------------------------------------------------------------
# SMARTER_DEBUG_MODE. (REQUIRED) Used locally to control logging verbosity
# and also determines how static files are served.
#------------------------------------------------------------------------------
# SMARTER_DEBUG_MODE=false

#------------------------------------------------------------------------------
# SMARTER_ENABLE_DASHBOARD_SERVER_LOGS. (OPTIONAL) Used to enable or disable
# the dashboard server logs feature.
#------------------------------------------------------------------------------
SMARTER_ENABLE_DASHBOARD_SERVER_LOGS=true

#------------------------------------------------------------------------------
# SMARTER_ENABLE_VECTORSTORE. (OPTIONAL) Used to enable or disable
# the vectorstore feature.
#------------------------------------------------------------------------------
SMARTER_ENABLE_VECTORSTORE=true


###############################################################################
# Django Overrides. variables prefixed with DJANGO_ are used to override any
# existing Django settings value. Values that you set here will be consumed,
# cast and assigned near the bottom of smarter/settings/base.py. These
# will override the Smarter and the Django default settings.
#
# Read more here: https://docs.djangoproject.com/en/6.0/ref/settings/
###############################################################################

#------------------------------------------------------------------------------
# DJANGO_AUTHENTICATION_BACKENDS. (OPTIONAL) Comma-separated list of Django authentication backends to use.
#
# Possible values:
# - social_core.backends.github.GithubOAuth2
# - social_core.backends.google.GoogleOAuth2
# - django.contrib.auth.backends.ModelBackend
# - smarter.lib.social_core.backends.multitenant.GoogleOAuth2Multitenant
# - smarter.lib.social_core.backends.multitenant.GithubOAuth2Multitenant
# - smarter.lib.social_core.backends.multitenant.DjangoModelBackendMultitenant
#
# Comma-separated list of Django authentication backends to use.
# The default value includes Google OAuth2, GitHub OAuth2,
# and the default Django ModelBackend, each with optional multitenant support.
#
# The order of backends matters. Django will attempt to authenticate
# using each backend in the order they are listed until one succeeds.
#
# If oauth backends are used, and, the corresponding oauth keys are set
# in smarter_settings, the web console login page will display social login
# buttons for each oauth backend.
#------------------------------------------------------------------------------
# DJANGO_AUTHENTICATION_BACKENDS=smarter.lib.social_core.backends.multitenant.GoogleOAuth2Multitenant,smarter.lib.social_core.backends.multitenant.GithubOAuth2Multitenant,smarter.lib.social_core.backends.multitenant.DjangoModelBackendMultitenant

#------------------------------------------------------------------------------
# DJANGO_SECRET_KEY. (OPTIONAL) Django secret key. Used for cryptographic signing in Django.
#
# - It should be a long, random, and unique string (at least 50 characters recommended).
# - It must contain a mix of letters, numbers, and symbols for high entropy.
# - It should be kept secret and never shared or committed to version control.
# - It is used for session signing, password reset tokens, and other security-related operations.
#
# If unset (default for local development), a warning will be logged and Django
# will generate a proxy secret key at runtime. This is fine when running in
# developer mode locally, but is NOT suitable for production deployments.
#------------------------------------------------------------------------------
# DJANGO_SECRET_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# DJANGO_ADMINS. (OPTIONAL) Comma-separated list of tuples, consisting of name
# and email addresses for Django admins.
#
# This is used by Django to send error notifications to site administrators.
# see: https://docs.djangoproject.com/en/6.0/ref/settings/#admins
#------------------------------------------------------------------------------
# DJANGO_ADMINS=[("Your Name", "your_email@example.com"),("Another Admin", "another_admin@example.com"),]


###############################################################################
#                             Smarter Settings
###############################################################################
#
# Note that Smarter Settings are denoted by the 'SMARTER_' prefix. All of these
# are consumed by smarter.common.conf at application startup. This module
# instantiates an immutable singleton instance of pydantic.BaseSettings
# named smarter_settings which strips off the SMARTER_ prefix and implements the
# setting in lower-case snake case. smarter_settings is the exclusive provider
# of all configuration settings in the Smarter codebase, often setting the
# corresponding Django settings where applicable.
#
# Example:
#  At runtime, the .env variable SMARTER_ROOT_DOMAIN
#  becomes smarter_settings.root_domain, and is accessible in the
#  codebase as follows:
#
#    from smarter.common.conf import smarter_settings
#    print(smarter_settings.root_domain)
###############################################################################

#------------------------------------------------------------------------------
# SMARTER_FERNET_ENCRYPTION_KEY (OPTIONAL) -> smarter_settings.fernet_encryption_key
# Used for Pydantic SecretStr encryption.
# Must be a URL-safe base64-encoded 32-byte key.
# see: https://fernetkeygen.com/
#
# If unset, a warning will be logged and a temporary key
# will be generated at runtime. This is fine when running
# in developer mode locally, but is NOT suitable for production deployments.
#------------------------------------------------------------------------------
# SMARTER_FERNET_ENCRYPTION_KEY="SET-ME-PLEASE"

#------------------------------------------------------------------------------
# SMARTER_GEMINI_API_KEY (OPTIONAL) -> smarter_settings.gemini_api_key
# Smarter Gemini API key. This is a required setting.
# Google Gemini is a built-in LLM Provider.
#
# see: https://aistudio.google.com/app/apikey
#------------------------------------------------------------------------------
# SMARTER_GEMINI_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_LLAMA_API_KEY (OPTIONAL) -> smarter_settings.llama_api_key
# Llama API key. This is a required setting.
# Meta Llama 2 is a built-in LLM Provider.
#
# see: https://console.apillm.com/en/dashboard/api-token
#------------------------------------------------------------------------------
# SMARTER_LLAMA_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_ANTHROPIC_API_KEY (OPTIONAL) -> smarter_settings.anthropic_api_key
# Anthropic API key.
# see: https://docs.anthropic.com/en/api/getting-started
#------------------------------------------------------------------------------
# SMARTER_ANTHROPIC_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_COHERE_API_KEY (OPTIONAL) -> smarter_settings.cohere_api_key
# Cohere API key.
# see: https://dashboard.cohere.com/api-keys
#------------------------------------------------------------------------------
# SMARTER_COHERE_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_FIREWORKS_API_KEY (OPTIONAL) -> smarter_settings.fireworks_api_key
# Fireworks AI API key.
# see: https://fireworks.ai/api-keys/
#------------------------------------------------------------------------------
# SMARTER_FIREWORKS_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_FILE_DROP_ZONE_ENABLED (OPTIONAL) -> smarter_settings.file_drop_zone_enabled
# Enables the file drop zone feature in the Smarter web console.
# When enabled, users can drag and drop manifest files into the web console
# as an alternative to using the cli.
#------------------------------------------------------------------------------
SMARTER_FILE_DROP_ZONE_ENABLED=true

#------------------------------------------------------------------------------
# SMARTER_MISTRAL_API_KEY (OPTIONAL) -> smarter_settings.mistral_api_key
# Mistral API key.
# see: https://console.mistral.ai/api-keys/
#------------------------------------------------------------------------------
# SMARTER_MISTRAL_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_TOGETHERAI_API_KEY (OPTIONAL) -> smarter_settings.togetherai_api_key
# TogetherAI API key.
# see: https://together.ai/settings/api-keys
#------------------------------------------------------------------------------
# SMARTER_TOGETHERAI_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_GOOGLE_SERVICE_ACCOUNT_B64 (REQUIRED) -> smarter_settings.google_service_account_b64
# Google Service Account for accessing
# Google Cloud resources.
# See https://console.cloud.google.com/projectselector2/iam-admin/serviceaccounts?supportedpurview=project
#
# Also see ./scripts/google-service-account.sh for technical details on how
# to encode a google service account credential as a base64 string.
#------------------------------------------------------------------------------
SMARTER_GOOGLE_SERVICE_ACCOUNT_B64=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_GOOGLE_MAPS_API_KEY (REQUIRED) -> smarter_settings.google_maps_api_key
# Google Maps API key.
# Used by the Smarter platform for implementing the OpenAI API get_weath()
# function calling example.
#
# see: https://platform.openai.com/docs/guides/function-calling
#------------------------------------------------------------------------------
SMARTER_GOOGLE_MAPS_API_KEY=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_SOCIAL_AUTH_GOOGLE_OAUTH2_KEY and SMARTER_SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET.
# (OPTIONAL)
# - smarter_settings.social_auth_google_oauth2_key
# - smarter_settings.social_auth_google_oauth2_secret
#
# Google OAuth2 credentials for social authentication.
# see: https://console.developers.google.com/apis/credentials
#------------------------------------------------------------------------------
# SMARTER_SOCIAL_AUTH_GOOGLE_OAUTH2_KEY=SET-ME-PLEASE
# SMARTER_SOCIAL_AUTH_GOOGLE_OAUTH2_SECRET=SET-ME-PLEASE


#------------------------------------------------------------------------------
# SMARTER_SOCIAL_AUTH_GITHUB_KEY and SMARTER_SOCIAL_AUTH_GITHUB_SECRET.
# (OPTIONAL)
# - smarter_settings.social_auth_github_key
# - smarter_settings.social_auth_github_secret
#
# GitHub OAuth2 credentials for social authentication.
# see: httpshttps://docs.github.com/en/apps/oauth-apps/building-oauth-apps/creating-an-oauth-app
#------------------------------------------------------------------------------
# SMARTER_SOCIAL_AUTH_GITHUB_KEY=SET-ME-PLEASE
# SMARTER_SOCIAL_AUTH_GITHUB_SECRET=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_MARKETING_SITE_URL (OPTIONAL) -> smarter_settings.marketing_site_url
# The public marketing URL of your Smarter deployment.
# Used in Django web and email templates for identity and branding purposes.
#------------------------------------------------------------------------------
# SMARTER_MARKETING_SITE_URL="http://localhost:8000"

#------------------------------------------------------------------------------
# SMARTER_LOGO (OPTIONAL) -> smarter_settings.logo
# URL to the logo image used in the Smarter web console.
# Used in Django web templates for identity and branding purposes.
#------------------------------------------------------------------------------
# SMARTER_LOGO="https://example.com/logo.png"

#------------------------------------------------------------------------------
# SMARTER_MYSQL_TEST_DATABASE_SECRET_NAME (OPTIONAL) -> smarter_settings.mysql_test_database_secret_name
# For unit tests. The name of the
# Smarter SAM Secret that contains the test database password credential.
#
# The Smarter test database is a a remote MySql database used during
# unit tests to validate SqlPlugin functionality.
#------------------------------------------------------------------------------
SMARTER_MYSQL_TEST_DATABASE_SECRET_NAME=smarter_test_db

#------------------------------------------------------------------------------
# SMARTER_MYSQL_TEST_DATABASE_PASSWORD (OPTIONAL) -> smarter_settings.mysql_test_database_password
# For unit tests. The password for the Smarter test database.
#
# The Smarter test database is a a remote MySql database used during
# unit tests to validate SqlPlugin functionality.
#------------------------------------------------------------------------------
SMARTER_MYSQL_TEST_DATABASE_PASSWORD=smarter_test_user

#------------------------------------------------------------------------------
# SMARTER_REACTJS_APP_LOADER_PATH (OPTIONAL) -> smarter_settings.reactjs_app_loader_path
# Path to the ReactJS app loader script that
# loads the Smarter Chat app into the Smarter Prompt Engineer Workbench.
#
# The React app component originates from
#  - https://github.com/smarter-sh/smarter-chat and
#  - https://github.com/smarter-sh/web-integration-example
# and is built-deployed to AWS Cloudfront. The React app is loaded from
# a url like: https://cdn.alpha.platform.smarter.sh/ui-chat/index.html
#------------------------------------------------------------------------------
# SMARTER_REACTJS_APP_LOADER_PATH="/ui-chat/app-loader.js"

#------------------------------------------------------------------------------
# SMARTER_SETTINGS_OUTPUT (OPTIONAL) -> smarter_settings.settings_output
# Enables detailed settings output to the console
# during startup and management commands. Specifically, it will output a list
# of all smarter_settings values that have been overriden by environment variables
# in this .env file or other environment sources.
#------------------------------------------------------------------------------
SMARTER_SETTINGS_OUTPUT=true

#------------------------------------------------------------------------------
# SMARTER_SMTP_SENDER (OPTIONAL) -> smarter_settings.smtp_sender
# SMTP email sender hostname.
#
# NOTE: If any of the SMTP credentials are missing, email sending
# will be quasi-silently disabled. Smarter will generate a warning log
# with the contents of the email that would have been sent.
#------------------------------------------------------------------------------
# SMARTER_SMTP_SENDER=local.platform.example.com

#------------------------------------------------------------------------------
# SMARTER_SMTP_PASSWORD (OPTIONAL) -> smarter_settings.smtp_password
# SMTP email password. Assumed to be the AWS SES SMTP
# secret access key if using AWS SES as the SMTP email provider.
#------------------------------------------------------------------------------
# SMARTER_SMTP_PASSWORD=SET-ME-PLEASE


#------------------------------------------------------------------------------
# SMARTER_SMTP_USERNAME (OPTIONAL) -> smarter_settings.smtp_username
# Assumed to be the AWS SES SMTP username if using
# AWS SES as the SMTP email provider.
#------------------------------------------------------------------------------
# SMARTER_SMTP_USERNAME=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_SMTP_FROM_EMAIL (OPTIONAL) -> smarter_settings.smtp_from_email
# SMTP email from address. Used as the default
# from address for all outgoing emails from the Smarter platform.
#------------------------------------------------------------------------------
# SMARTER_SMTP_FROM_EMAIL=no-reply@alpha.platform.example.com

#------------------------------------------------------------------------------
# SMARTER_MAILCHIMP_API_KEY and SMARTER_MAILCHIMP_LIST_ID.
# (OPTIONAL)
#  - smarter_settings.mailchimp_api_key
#  - smarter_settings.mailchimp_list_id
#
# Mailchimp API key for Smarter marketing email integration.
# see: https://mailchimp.com/developer/marketing/guides/quick-start/
#------------------------------------------------------------------------------
# SMARTER_MAILCHIMP_API_KEY=SET-ME-PLEASE
# SMARTER_MAILCHIMP_LIST_ID=SET-ME-PLEASE

#------------------------------------------------------------------------------
# SMARTER_PINECONE_API_KEY and SMARTER_PINECONE_ENVIRONMENT
# (OPTIONAL)
# - smarter_settings.pinecone_api_key
# - smarter_settings.pinecone_environment
#
# Pinecone API key and environment for vector database integration.
# see: https://docs.pinecone.io/docs/get-started
#------------------------------------------------------------------------------
# SMARTER_PINECONE_API_KEY=SET-ME-PLEASE
# SMARTER_PINECONE_ENVIRONMENT=SET-ME-PLEASE

# -----------------------------------------------------------------------------
# SMARTER_ALLOWED_HOSTS (OPTIONAL) -> smarter_settings.allowed_hosts
# Django setting override.
# Supplemental list of allowed host/domain names for Smarter LLMClients/Agents.
# -----------------------------------------------------------------------------
# SMARTER_ALLOWED_HOSTS=[]


# -----------------------------------------------------------------------------
# SMARTER_INTERNAL_IP_PREFIXES (OPTIONAL) -> smarter_settings.internal_ip_prefixes
# Django setting override.
#
# Supplemental list of internal IP prefixes used in
# smarter.apps.llm_client.middleware.security.SmarterSecurityMiddleware
# and smarter.lib.django.middleware security features. This primarily helps
# Django identify Kubernetes readiness, liveness, and health check probes
# as internal requests that can be ignored for certain security checks.
#
# The default value is based on the default internal IP range created by
# https://github.com/smarter-sh/smarter-infrastructure and used by Kubernetes
# by default unless otherwise configured.
# -----------------------------------------------------------------------------
# SMARTER_INTERNAL_IP_PREFIXES=["192.168."]


# -----------------------------------------------------------------------------
# SMARTER_API_SCHEMA (REQUIRED) -> smarter_settings.api_schema
# Smarter API schema (http or https).
# -----------------------------------------------------------------------------
# SMARTER_API_SCHEMA="http"

# -----------------------------------------------------------------------------
# SMARTER_API_NAME (OPTIONAL) -> smarter_settings.api_name
# Name of the Smarter API.
# Used in API documentation and OpenAPI schema generation.
# -----------------------------------------------------------------------------
# SMARTER_API_NAME="Smarter API"

# -----------------------------------------------------------------------------
# SMARTER_API_DESCRIPTION (OPTIONAL) -> smarter_settings.api_description
# Description of the Smarter API.
# Used in API documentation and OpenAPI schema generation.
# -----------------------------------------------------------------------------
# SMARTER_API_DESCRIPTION="An enterprise class plugin-based AI llm_client platform"
# Description of the Smarter API. Not currently used.

###############################################################################
# Marketing and branding settings
###############################################################################

# -----------------------------------------------------------------------------
# SMARTER_BRANDING_CORPORATE_NAME (OPTIONAL) -> smarter_settings.branding_corporate_name
# SMARTER_BRANDING_SUPPORT_PHONE_NUMBER (OPTIONAL) -> smarter_settings.branding_support_phone_number
# SMARTER_BRANDING_SUPPORT_EMAIL (OPTIONAL) -> smarter_settings.branding_support_email
# SMARTER_BRANDING_ADDRESS1 (OPTIONAL) -> smarter_settings.branding_address1
# SMARTER_BRANDING_ADDRESS2 (OPTIONAL) -> smarter_settings.branding_address2
# SMARTER_BRANDING_CITY (OPTIONAL) -> smarter_settings.branding_city
# SMARTER_BRANDING_STATE (OPTIONAL) -> smarter_settings.branding_state
# SMARTER_BRANDING_POSTAL_CODE (OPTIONAL) -> smarter_settings.branding_zip_code
# SMARTER_BRANDING_COUNTRY (OPTIONAL) -> smarter_settings.branding_country
# SMARTER_BRANDING_CURRENCY (OPTIONAL) -> smarter_settings.branding_currency
# SMARTER_BRANDING_TIMEZONE (OPTIONAL) -> smarter_settings.branding_timezone
# SMARTER_BRANDING_CONTACT_URL (OPTIONAL) -> smarter_settings.branding_contact_url
# SMARTER_BRANDING_SUPPORT_HOURS (OPTIONAL) -> smarter_settings.branding_support_hours
# SMARTER_BRANDING_URL_FACEBOOK (OPTIONAL) -> smarter_settings.branding_url_facebook
# SMARTER_BRANDING_URL_TWITTER (OPTIONAL) -> smarter_settings.branding_url_twitter
# SMARTER_BRANDING_URL_LINKEDIN (OPTIONAL) -> smarter_settings.branding_url_linkedin
# SMARTER_EMAIL_ADMIN (OPTIONAL) -> smarter_settings.email_admin
#
# The corporate name used in Smarter branding.
# Provided to the Smarter web console dashboard
# context processor for use in the common footer of html templates.
# -----------------------------------------------------------------------------
# SMARTER_BRANDING_CORPORATE_NAME="The Smarter Project"
# SMARTER_BRANDING_SUPPORT_PHONE_NUMBER="+1 (555) 123-4567"
# SMARTER_BRANDING_SUPPORT_EMAIL="support@example.com"
# SMARTER_BRANDING_ADDRESS1="1234 Smarter St, Smarter City, SM 12345"
# SMARTER_BRANDING_ADDRESS2="Suite 100"
# SMARTER_BRANDING_CITY="Smarter City"
# SMARTER_BRANDING_STATE="SM"
# SMARTER_BRANDING_POSTAL_CODE="12345"
# SMARTER_BRANDING_COUNTRY="Smarterland"
# SMARTER_BRANDING_TIMEZONE="America/Denver"
# SMARTER_BRANDING_CURRENCY="USD"
# SMARTER_BRANDING_CONTACT_URL="http://example.com/contact-us"
# SMARTER_BRANDING_SUPPORT_HOURS="MON-FRI 9:00 AM - 5:00 PM GMT-6 (CST)"
# SMARTER_BRANDING_URL_FACEBOOK="https://www.facebook.com/example"
# SMARTER_BRANDING_URL_TWITTER="https://twitter.com/example"
# SMARTER_BRANDING_URL_LINKEDIN="https://www.linkedin.com/company/example"
# SMARTER_EMAIL_ADMIN="support@example.com"

###############################################################################
# Chat and LLMClient settings
###############################################################################

# -----------------------------------------------------------------------------
# SMARTER_CHAT_CACHE_EXPIRATION (OPTIONAL) -> smarter_settings.chat_cache_expiration
#
# Defaults to 5 seconds.
# Just enough to fend off a DDOS attack.
# Default cache expiration time for chat message related views. Intended to
# act as a mild deterrent against DDOS attacks.
# see: :class:`smarter.apps.prompt.models.PromptHelper`
# -----------------------------------------------------------------------------
# SMARTER_CHAT_CACHE_EXPIRATION=5

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_CACHE_EXPIRATION (OPTIONAL) -> smarter_settings.llm_client_cache_expiration
# Defaults to 5 minutes.
# Default cache expiration time for LLMClient related views.
# Not currently in use.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_CACHE_EXPIRATION=60 * 5  # 5 minutes

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_MAX_RETURNED_HISTORY (OPTIONAL) -> smarter_settings.llm_client_max_returned_history
# Defaults to 25 messages.
# Maximum number of chat history messages to return in API responses.
# Not currently in use.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_MAX_RETURNED_HISTORY=25

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_DNS_RECORD (OPTIONAL) -> smarter_settings.llm_client_tasks_create_dns_record
# For programmatically creating DNS records in AWS Route53 during LLMClient deployment.
# Set these to true if we *DO NOT* place a wildcard A record in the customer API domain
# requiring that every llm_client have its own A record. This is the default behavior.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_DNS_RECORD=True

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_INGRESS_MANIFEST (OPTIONAL) -> smarter_settings.llm_client_tasks_create_ingress_manifest
# Set this to true if we intend to create an ingress manifest for the customer API domain
# so that we can issue a certificate for it.
#
# For programmatically creating Kubernetes ingress manifests and TLS
# certificates for LLMClients during deployment.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_INGRESS_MANIFEST=True

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_DEFAULT_TTL (OPTIONAL) -> smarter_settings.llm_client_tasks_default_ttl
# For programmatically creating DNS records in AWS Route53.
# set the TTL for the DNS record.
#
# Default TTL (time to live) for DNS records created in AWS Route53 during LLMClient deployment.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_DEFAULT_TTL=600

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_MAX_RETRIES (OPTIONAL) -> smarter_settings.llm_client_tasks_celery_max_retries
# Celery worker task configuration Maximum number of retries for Celery tasks
# related to LLMClient deployment and management.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_MAX_RETRIES=3

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_RETRY_BACKOFF (OPTIONAL) -> smarter_settings.llm_client_tasks_celery_retry_backoff
# If True, enables exponential backoff for Celery task retries related to
# LLMClient deployment and management.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_RETRY_BACKOFF=True

# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_TASK_QUEUE (OPTIONAL) -> smarter_settings.llm_client_tasks_celery_task_queue
# Name of the Celery task queue for LLMClient deployment and management tasks.
# The default value should be sufficient for most deployments.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CELERY_TASK_QUEUE="default_celery_task_queue"


# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_DNS_RECORD (OPTIONAL) -> smarter_settings.llm_client_tasks_create_dns_record
# For programmatically creating and applying Kubernetes ingress manifests
# during LLMClient deployment.
# -----------------------------------------------------------------------------
# SMARTER_LLM_CLIENT_TASKS_CREATE_INGRESS_MANIFEST=True

###############################################################################
# Plugin settings
###############################################################################

# -----------------------------------------------------------------------------
# SMARTER_PLUGIN_MAX_DATA_RESULTS (OPTIONAL) -> smarter_settings.plugin_max_data_results
# A global maximum number of data row results that can be returned by any
# Smarter plugin. This is intended to act as a safeguard against
# runaway queries that return massive result sets.
# -----------------------------------------------------------------------------
# SMARTER_PLUGIN_MAX_DATA_RESULTS=50

# -----------------------------------------------------------------------------
# SMARTER_SENSITIVE_FILES_AMNESTY_PATTERNS (OPTIONAL) -> smarter_settings.sensitive_files_amnesty_patterns
# Sensitive file amnesty patterns used by
# smarter.lib.django.middleware.sensitive_files.SensitiveFileAccessMiddleware.
# Requests matching these patterns will be allowed even if they match sensitive file names.
#
# Do not modify this setting unless you fully understand the implications of doing so.
# -----------------------------------------------------------------------------
# SMARTER_SENSITIVE_FILES_AMNESTY_PATTERNS="^/dashboard/account/password-reset-link/[^/]+/[^/]+/$,^/api(/.*)?$,^/admin(/.*)?$,^/plugin(/.*)?$,^/docs/manifest(/.*)?$,^/docs/json-schema(/.*)?$,.*stackademy.*,^/\.well-known/acme-challenge(/.*)?$"

###############################################################################
# SMARTER_VERBOSE_LOGGING (OPTIONAL) -> smarter_settings.verbose_logging
# Enables verbose logging for the Smarter platform. When enabled, additional
# debug information will be included in the logs.
###############################################################################
# SMARTER_VERBOSE_LOGGING=true

###############################################################################
# FOR TESTING HELM CHARTS
# Kubernetes secrets - smarter-platform-alpha
###############################################################################
# SMARTER_AWS_EKS_CLUSTER_NAME=SET-ME-PLEASE
# SMARTER_AWS_RDS_DB_INSTANCE_IDENTIFIER=SET-ME-PLEASE
# SMARTER_NAMESPACE=smarter-platform-alpha
# SMARTER_MYSQL_HOST=SET-ME-PLEASE
# SMARTER_MYSQL_PORT=3306
# SMARTER_MYSQL_DATABASE=SET-ME-PLEASE
# SMARTER_MYSQL_PASSWORD=SET-ME-PLEASE
# SMARTER_MYSQL_USERNAME=SET-ME-PLEASE
# SMARTER_MYSQL_ROOT_USERNAME=SET-ME-PLEASE
# SMARTER_MYSQL_ROOT_PASSWORD=SET-ME-PLEASE
# SMARTER_LOGIN_URL=https://alpha.platform.example.com/login/
# SMARTER_ADMIN_PASSWORD=SET-ME-PLEASE
# SMARTER_ADMIN_USERNAME=admin
# SMARTER_DOCKER_IMAGE=012345678903.dkr.ecr.us-east-1.amazonaws.com/smarter-platform-alpha:latest
